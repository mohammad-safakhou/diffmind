{
  "name": "docker-compose-identity",
  "description": "Extract service identity from Docker Compose files",
  "version": "v1",
  "applies_to": {
    "kind": "any",
    "match": {
      "has_file": "docker-compose.yml"
    }
  },
  "extractions": [
    {
      "name": "compose_services",
      "description": "Extract service definitions from Docker Compose",
      "source": {
        "glob": "docker-compose*.y*ml"
      },
      "strategy": "llm",
      "prompt_hint": "Extract service names, ports, environment variables containing URLs or connection strings, and network aliases from this Docker Compose file.",
      "extract": [
        {"maps_to": "service_name"},
        {"maps_to": "dns_aliases"},
        {"maps_to": "database_connection"},
        {"maps_to": "queue_identifiers"}
      ]
    }
  ]
}
