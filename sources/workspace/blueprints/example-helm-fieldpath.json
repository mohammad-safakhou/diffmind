{
  "name": "example-helm-fieldpath",
  "description": "Deterministic field extraction from .example Helm values for Example services",
  "version": "v1",
  "applies_to": {
    "kind": "service_repo",
    "match": {
      "has_path": ".example"
    }
  },
  "extractions": [
    {
      "name": "production_identity",
      "description": "Extract IAM role, ingress subdomain, and env vars from production Helm values",
      "source": {
        "glob": ".example/config/production/values.yaml"
      },
      "strategy": "field_path",
      "extract": [
        {"field": "iamRole", "maps_to": "iam_role"},
        {"field": "ingress.default.subdomain", "maps_to": "dns_aliases"},
        {"field": "configMaps.configmap.env", "maps_to": "service_env"}
      ]
    },
    {
      "name": "base_identity",
      "description": "Extract service port and ingress config from base Helm values",
      "source": {
        "glob": ".example/config/values.yaml"
      },
      "strategy": "field_path",
      "extract": [
        {"field": "ingress.default.subdomain", "maps_to": "dns_aliases"},
        {"field": "service.ports.default.containerPort", "maps_to": "container_port"}
      ]
    }
  ]
}
